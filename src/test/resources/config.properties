# Base
base.url=https://automationintesting.online/api

# Endpoints (NO /api here)
health.endpoint=/booking/actuator/health
auth.endpoint=/auth/login
booking.endpoint=/booking

# Auth
auth.username=admin
auth.password=password